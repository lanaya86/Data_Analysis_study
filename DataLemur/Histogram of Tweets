--https://datalemur.com/questions/sql-histogram-tweets

SELECT user_tweet_count AS tweet_bucket, COUNT(*) AS users_num
  
FROM (
  SELECT COUNT(tweet_id) AS user_tweet_count
  FROM tweets
  WHERE tweet_date >= '2022-01-01'
    GROUP BY user_id
) AS user_tweet_counts

GROUP BY user_tweet_count;
